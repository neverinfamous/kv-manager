# v2.2.8

- Routine dependency updates and security patches.

## Changed

- **Dependency Updates**
  - Bumped `jose` to `^6.2.3` and `lucide-react` to `^1.14.0`
  - Bumped dev dependencies: `@cloudflare/workers-types`, `eslint`, `globals`, `postcss`, `typescript-eslint`, `vite`, `wrangler`
  - Bumped `tar` in Dockerfile and overrides to `7.5.14`

---

**Install/Update:**
`docker pull writenotenow/kv-manager:v2.2.8`
