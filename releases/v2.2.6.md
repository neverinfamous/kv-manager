# v2.2.6 — Security & Dependency Patch

## Highlights

- Patched three transitive vulnerability chains: `picomatch` Method Injection, `flatted` Prototype Pollution, and `brace-expansion` DoS
- Upgraded `vite` to 8.0.5, `eslint` to 10.2.0, and `wrangler` to 4.80.0
- Updated `lucide-react` to 1.7.0 and `jose` to 6.2.2
- Standardized `CHANGELOG.md` to strict Keep a Changelog compliance (merged duplicate sections, added all missing compare URLs)

## Security

- Fixed `picomatch` Method Injection vulnerability via exact pins and Docker build layer patching
- Fixed `flatted` Prototype Pollution vulnerability via exact pins
- Fixed `brace-expansion` DoS vulnerability

## Changed

- Bumped `vite` to `8.0.5`, `eslint` to `10.2.0`, `wrangler` to `4.80.0`, `lucide-react` to `1.7.0`, `jose` to `6.2.2`
- Updated `@cloudflare/workers-types`, `@types/node`, `@tailwindcss/postcss`, `tailwindcss`, `typescript`, `typescript-eslint`

---

**Compare:** [v2.2.5...v2.2.6](https://github.com/neverinfamous/kv-manager/compare/v2.2.5...v2.2.6)

**Docker:** `docker pull writenotenow/kv-manager:v2.2.6`
