# Cloudflare KV Manager v2.2.3

## Highlights

- **Changelog Migrated** — Moved `Changelog.md` from the wiki repository into the main project root as `CHANGELOG.md` for easier access and discoverability
- **Updated Documentation Links** — README and Docker README now link to the root changelog instead of the wiki
- **Docker Hub Description Fix** — Fixed workflow conditions so Docker Hub README actually updates on tag pushes

## Documentation

- Moved `Changelog.md` from `kv-manager.wiki` to project root as `CHANGELOG.md`
- Updated changelog links in `README.md` and `DOCKER_README.md` to point to root `CHANGELOG.md`
- Removed `VERSION` file — version now derived strictly from `package.json`

## CI/CD

- Fixed `Update Docker Hub Description` and `Deployment Summary` steps in `docker-publish.yml` to trigger on tag pushes instead of branch pushes

---

**Full Changelog:** [`v2.2.2...v2.2.3`](https://github.com/neverinfamous/kv-manager/compare/v2.2.2...v2.2.3)

**Install:**

```bash
docker pull writenotenow/kv-manager:v2.2.3
```
